{
  "version": 2,
  "github": {
    "enabled": false
  },
  "builds": [
    { "src": ".", "use": "@vercel/static-build", "config": { "buildCommand": "npm run build" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}